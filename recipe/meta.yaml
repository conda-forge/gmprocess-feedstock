{% set version = "0.2.2" %}

package:
  name: gmprocess
  version: {{ version }}

source:
  url: https://github.com/usgs/groundmotion-processing/archive/{{ version }}.tar.gz
  sha256: fd474c195b89b0e47e18851a94c9fa45247b77c37dc4a6806e58e05dfa679f7a

build:
  number: 0
  script: "{{ PYTHON }} -m pip install . --no-deps -vv"
  skip: true  # [win]

requirements:
  build:
    - {{ compiler('c') }}
  host:
    - python >=3.6
    - cython
    - numpy
    - pip
  run:
    - libcomcat
    - prov
    - scipy
    - obspy >=1.1.1
    - openpyxl
    - pytest
    - setuptools
    - requests
    - matplotlib
    - h5py
    - pandas
    - lxml
    - vcrpy
    - openquake.engine
    - impactutils
    - cartopy
    - pytz
    - beautifulsoup4
    - pyasdf
    - python
    - pyyaml
    - {{ pin_compatible('numpy') }}

test:
  imports:
    - gmprocess

about:
  home: https://github.com/usgs/groundmotion-processing
  license: Public Domain
  summary: 'Fetch and process strong motion waveform/peak amplitude data'
  license_family: Public Domain
  license_file: LICENSE.md

extra:
  recipe-maintainers:
    - mhearne-usgs
    - hschovanec-usgs
    - emthompson-usgs
