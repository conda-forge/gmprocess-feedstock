{% set version = "1.1.2" %}

package:
  name: gmprocess
  version: {{ version }}

source:
  url: https://github.com/usgs/groundmotion-processing/archive/v{{ version }}.tar.gz
  sha256: 10b334a67f55de31f131d2d65a6d27153f194b8642ea38d23783f5b0c1232c01

build:
  number: 0
  skip: true  # [win or py27]
  script: {{ PYTHON }} -m pip install . --no-deps -vv

requirements:
  build:
    - {{ compiler('c') }}
  host:
    - python
    - cython
    - numpy
    - pip
  run:
    - python
    - setuptools
    - {{ pin_compatible('numpy') }}
    - beautifulsoup4
    - cartopy
    - h5py >=2.8.0
    - impactutils
    - libcomcat
    - lxml
    - matplotlib-base
    - obspy >=1.1.1
    - openpyxl
    - openquake.engine
    - pandas
    - prov
    - pyasdf
    - pytest
    - pytz
    - pyyaml
    - requests
    - scipy
    - vcrpy

test:
  imports:
    - gmprocess

about:
  home: https://github.com/usgs/groundmotion-processing
  license: Public Domain
  summary: Fetch and process strong motion waveform/peak amplitude data
  license_family: Public Domain
  license_file: LICENSE.md

extra:
  recipe-maintainers:
    - mhearne-usgs
    - hschovanec-usgs
    - emthompson-usgs
