{% set version = "0.2.9" %}

package:
  name: gmprocess
  version: {{ version }}

source:
  url: https://github.com/usgs/groundmotion-processing/archive/{{ version }}.tar.gz
  sha256: bb741b9cd91a850523294623930a8eaa6be6f6039e31c83d98e1e9336c3d7381

build:
  number: 0
  script: "{{ PYTHON }} -m pip install . --no-deps -vv"
  skip: true  # [win or py27]

requirements:
  build:
    - {{ compiler('c') }}
  host:
    - python
    - cython
    - numpy
    - pip
  run:
    - libcomcat
    - prov
    - scipy
    - obspy >=1.1.1
    - openpyxl
    - pytest
    - setuptools
    - requests
    - matplotlib
    - h5py >=2.8.0
    - pandas
    - lxml
    - vcrpy
    - openquake.engine
    - impactutils
    - cartopy
    - pytz
    - beautifulsoup4
    - pyasdf
    - python
    - pyyaml
    - {{ pin_compatible('numpy') }}

test:
  imports:
    - gmprocess

about:
  home: https://github.com/usgs/groundmotion-processing
  license: Public Domain
  summary: 'Fetch and process strong motion waveform/peak amplitude data'
  license_family: Public Domain
  license_file: LICENSE.md

extra:
  recipe-maintainers:
    - mhearne-usgs
    - hschovanec-usgs
    - emthompson-usgs
